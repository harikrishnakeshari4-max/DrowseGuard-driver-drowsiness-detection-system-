# MongoDB Atlas connection string
# Replace <username>, <password> and <cluster-url> with your Atlas values.
MONGO_URL=mongodb+srv://<username>:<password>@<cluster-url>/drowsiness_db?retryWrites=true&w=majority

# Optional database name. Defaults to drowsiness_db.
MONGO_DB_NAME=drowsiness_db
